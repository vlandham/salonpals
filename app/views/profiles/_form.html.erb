<%= simple_form_for(@profile) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.association :user_id %>
    <%= f.input :description %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
