<h1>Listing profiles</h1>

<table>
<% @profiles.each do |profile| %>
  <tr>
    <td><%=h profile.user.first_name %></td>
    <td><%=h profile.user.last_name %></td>
    <td><%=h profile.description %></td>
    <td><%= link_to 'Show', profile %></td>
  </tr>
<% end %>
</table>
