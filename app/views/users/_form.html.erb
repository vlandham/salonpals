<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%=h f.input :first_name %>
    <%=h f.input :last_name %>    
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
